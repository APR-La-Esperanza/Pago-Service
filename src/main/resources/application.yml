server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: pago-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-still-credit-apcj95x5-pooler.c-7.us-east-1.aws.neon.tech/apr_pago_db?sslmode=require&channel_binding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_m8wsGAxJlfI2}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true

service:
  socio:
    url: ${SERVICE_SOCIO_URL:http://socio-service}
  facturacion:
    url: ${SERVICE_FACTURACION_URL:http://facturacion-service}
