server:
  port: 8084

spring:
  application:
    name: pago-service

  datasource:
    url: jdbc:postgresql://ep-still-credit-apcj95x5-pooler.c-7.us-east-1.aws.neon.tech/apr_pago_db?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_m8wsGAxJlfI2

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
